<?php

# Load the textmagic library when the spark is loaded
$autoload['libraries'] = array('textmagic');

# Load the textmagic config when the spark is loaded
$autoload['config'] = array('textmagic');
